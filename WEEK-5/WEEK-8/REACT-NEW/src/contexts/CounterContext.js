// its not a component so it can be a normal java script file only

// step-1:- Create context Object--->(context obj contains nothing)
import { createContext } from "react";
export const CounterContext= createContext()
        // we need to provide state to that context object for the provider
        // for that we have created CounterContextProvider.jsx

// step-2:- Create context Object

// step-3:- Create context Object

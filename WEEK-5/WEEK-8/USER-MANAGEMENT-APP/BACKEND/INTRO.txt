CREATE BACKEND WITH USER REST API WITH BRLOW OPERATIONS
    1. Create new USER
    2. Read all users
    3. Read a User by ID
    4. Delete a user by ID
    

    User Schema --> {name,email,date of birth,mobile number}

 Create a React app to work with above API

                        App
            |       |           |           |
            |       |           |           |
            Home   AddUser   UsersList     User

        
Home component need to be loaded when app launch
After adding new User, it should navigate to UsersList
     automatically to display all Users( name,email)
When we click on User card in UsersList component, it
  should navigate to User component along with that user obj
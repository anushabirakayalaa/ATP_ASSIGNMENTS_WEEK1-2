STEPS
1. install tialwind css 
2. create new folder & gneterate packaghe.json
3. run-"npm installl tailwindcss @tailwindcss/cli"
4. create "style.css" file and add "@import "tailwindcss" in it
5. rum the watch command "npx @tailwindcss/cli -i ./style.css -o ./output.css --watch"
6. create HTML files and add <link href="./output.css>"
rel="stylesheet" > in head